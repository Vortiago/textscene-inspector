/**
 * CSGShape3D family registration: the own-geometry-degenerate rule shared by
 * all 7 concrete CSG types. Barrel-imported because the rule has no owning
 * leaf of its own (CSGShape3D is abstract).
 */

import './linter.js';
