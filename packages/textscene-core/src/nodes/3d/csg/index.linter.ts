/**
 * CSGShape3D family registration: the own-geometry-degenerate rule shared by every concrete CSG
 * type. Barrel-imported because the rule has no owning leaf (CSGShape3D is abstract).
 */

import './linter.js';
