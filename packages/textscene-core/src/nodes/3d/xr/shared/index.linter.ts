/**
 * OpenXRCompositionLayer tier registration: the shared validators and the family rule.
 * Barrel-imported because a rule has no leaf to pull it in.
 */

import './linterParser.js';
import './linter.js';
