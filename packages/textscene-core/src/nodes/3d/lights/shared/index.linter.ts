/**
 * Light3D shared-tier registration: the shared validators and the family rule.
 * Barrel-imported because the family rule has no owning leaf of its own.
 */

import './linterParser.js';
import './linter.js';
