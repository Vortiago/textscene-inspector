/**
 * Semantic linter rules for LightOccluder2D — none required.
 *
 * LightOccluder2D carries no semantic rules beyond property-format validation,
 * which is handled by the linterParser validators.
 */
