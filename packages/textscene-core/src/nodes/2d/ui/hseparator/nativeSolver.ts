/** HSeparator's minimum size: `separatorMinimumSize('horizontal')` (see `../separator/nativeSolver.ts`). */
import { separatorMinimumSize } from '../separator/nativeSolver';

export const hSeparatorMinimumSize = separatorMinimumSize('horizontal');
