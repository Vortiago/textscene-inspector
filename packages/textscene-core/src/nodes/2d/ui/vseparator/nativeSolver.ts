/** VSeparator's minimum size — `separatorMinimumSize('vertical')` (see `../separator/nativeSolver.ts`). */
import { separatorMinimumSize } from '../separator/nativeSolver';

export const vSeparatorMinimumSize = separatorMinimumSize('vertical');
