/**
 * Linter entry point for the shared AnimationMixer tier: registers the
 * `anims/<name>` / `libraries` / `libraries/<name>` format validators every
 * AnimationMixer descendant (AnimationPlayer, AnimationTree) inherits.
 */
import './linterParser.js';
