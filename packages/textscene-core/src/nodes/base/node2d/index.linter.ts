/**
 * Node2D linter registration: property validators only. Node2D is a base
 * class, and semantic validation lives in the subclasses (Sprite2D, Camera2D,
 * AnimatedSprite2D, …), so it has no `linter.ts` rule.
 */

import './linterParser.js';
