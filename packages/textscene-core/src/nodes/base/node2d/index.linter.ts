/**
 * Node2D linter registration — property validators only. Node2D is a base
 * class; semantic validation lives in the subclasses (Sprite2D, Camera2D,
 * AnimatedSprite2D, …), so there is deliberately no `linter.ts` rule here.
 */

import './linterParser.js';
