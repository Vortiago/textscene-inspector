/**
 * SubViewport registration — strict validators + semantic rules.
 * Imports only `.ts` (never `./Component`), preserving the React-free linter boundary.
 */

import './linterParser.js';
import './linter.js';
