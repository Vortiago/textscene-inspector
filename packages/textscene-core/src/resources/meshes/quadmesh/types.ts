/**
 * QuadMesh shares PlaneMesh's property shape (it is a PlaneMesh subclass in
 * Godot), re-exported here so the slice has its own type surface.
 */

export type { PlaneMeshProperties as QuadMeshProperties } from '../planemesh/types';
