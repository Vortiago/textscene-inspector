/** CapsuleShape2D decoded data. */

export interface CapsuleShape2DProperties {
  /** Capsule radius in pixels. `capsule_shape_2d.h:39`: `real_t radius = 10.0`. */
  radius: number;
  /**
   * Full height in pixels, semicircular caps included.
   * `capsule_shape_2d.h:38`: `real_t height = 30.0`.
   */
  height: number;
}
