/** ConvexPolygonShape3D decoded data. */

export interface ConvexPolygonShape3DProperties {
  /** Flat [x,y,z, x,y,z, ...] hull point cloud; build a ConvexGeometry from it. */
  points: Float32Array;
}
