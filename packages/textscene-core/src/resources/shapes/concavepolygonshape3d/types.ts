/** ConcavePolygonShape3D decoded data. */

export interface ConcavePolygonShape3DProperties {
  /** Flat [x,y,z, ...] triangle soup (every 3 vertices = 1 face); already triangulated. */
  data: Float32Array;
}
