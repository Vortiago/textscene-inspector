; The ONE variable: `internationalization/rendering/root_node_layout_direction`,
; set to 2 (Right-to-Left) in this directory's own project.godot. Every node
; here leaves `layout_direction` at INHERITED, so the climb runs off the top of
; the tree and this setting is the only thing that answers
; (control.cpp:3600-3608, seeded into the static by
; register_scene_types.cpp:568-570). Invisible in every loose fixture: without a
; project.godot the setting is 0 and the answer comes from the locale instead.

[gd_scene load_steps=1 format=3 uid="uid://unitctrlldroot"]

[node name="Root" type="Control"]
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0

[node name="Inherited" type="HBoxContainer" parent="."]
offset_left = 64.0
offset_top = 48.0
offset_right = 464.0
offset_bottom = 128.0

[node name="A" type="ColorRect" parent="Inherited"]
custom_minimum_size = Vector2(60, 0)
color = Color(0.9, 0.2, 0.2, 1)

[node name="B" type="ColorRect" parent="Inherited"]
custom_minimum_size = Vector2(120, 0)
color = Color(0.2, 0.7, 0.3, 1)

[node name="C" type="ColorRect" parent="Inherited"]
custom_minimum_size = Vector2(180, 0)
color = Color(0.2, 0.4, 0.9, 1)
