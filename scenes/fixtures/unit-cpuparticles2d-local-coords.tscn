[gd_scene load_steps=2 format=3]

; `local_coords` under a scale = Vector2(3, 3) emitter, where a still frame shows it. Global coords
; (the default) spawn in canvas space and draw at texture size. Local coords scale with the node.
; `preprocess = 0.95`, not 1.0: one full lifetime lands on the cycle's wrap, where every emitter has
; just recycled to the origin.

[ext_resource type="Texture2D" path="res://textures/pong-ball.png" id="1_ball"]

[node name="Root" type="Node2D"]

[node name="Backdrop" type="Polygon2D" parent="."]
color = Color(0.08, 0.08, 0.1, 1)
polygon = PackedVector2Array(320, 68, 832, 68, 832, 580, 320, 580)

[node name="GlobalCoords" type="CPUParticles2D" parent="."]
position = Vector2(460, 250)
scale = Vector2(3, 3)
amount = 16
texture = ExtResource("1_ball")
lifetime = 1.0
preprocess = 0.95
use_fixed_seed = true
seed = 21
fixed_fps = 30
local_coords = false
direction = Vector2(1, 0)
spread = 0.0
gravity = Vector2(0, 0)
initial_velocity_min = 60.0
initial_velocity_max = 60.0
scale_amount_min = 3.0
scale_amount_max = 3.0
color = Color(1, 0.6, 0.2, 1)

[node name="LocalCoords" type="CPUParticles2D" parent="."]
position = Vector2(460, 420)
scale = Vector2(3, 3)
amount = 16
texture = ExtResource("1_ball")
lifetime = 1.0
preprocess = 0.95
use_fixed_seed = true
seed = 21
fixed_fps = 30
local_coords = true
direction = Vector2(1, 0)
spread = 0.0
gravity = Vector2(0, 0)
initial_velocity_min = 60.0
initial_velocity_max = 60.0
scale_amount_min = 3.0
scale_amount_max = 3.0
color = Color(0.4, 0.8, 1, 1)
