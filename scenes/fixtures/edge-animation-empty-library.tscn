[gd_scene load_steps=2 format=3 uid="uid://edge_animation_empty_library"]

; A real (resolvable) AnimationLibrary SubResource with an empty `_data`
; dict — zero clips, but not a dangling reference. No autoplay/current_animation
; is set, so this lints completely clean (hasLibraries=true suppresses the
; 'no animations' warning; there is no clip reference to flag as missing).
; Render side: resolveAnimations returns [] — the player mounts, drives
; nothing, and its sibling renders normally.

[sub_resource type="AnimationLibrary" id="AnimationLibrary_empty"]
_data = {}

[node name="Scene" type="Node3D"]

[node name="Mesh" type="MeshInstance3D" parent="."]

[node name="AnimationPlayer" type="AnimationPlayer" parent="."]
libraries/ = SubResource("AnimationLibrary_empty")
