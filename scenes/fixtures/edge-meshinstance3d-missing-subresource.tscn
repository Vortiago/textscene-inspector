[gd_scene format=3 uid="uid://edge_meshinstance3d_missing_subresource"]

; `mesh` omitted. A dangling `mesh` reference errors (valid-meshinstance3d-resources) and breaks
; fixtureLint, so this pins the lint-clean case with the same render: the placeholder condition
; `!meshResource && !arrayMeshPath` holds for both, so a magenta wireframe draws.

[node name="Scene" type="Node3D"]

[node name="NoMesh" type="MeshInstance3D" parent="."]
