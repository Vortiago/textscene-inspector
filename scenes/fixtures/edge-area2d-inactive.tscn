[gd_scene load_steps=2 format=3 uid="uid://edge_area2d_inactive"]

; Area2D with both `monitoring` and `monitorable` false, so it detects nothing and nothing detects
; it: a warning (area2d-detects-nothing). Its gravity and damping overrides still apply, and it
; renders its shape normally.

[sub_resource type="CircleShape2D" id="CircleShape2D_1"]
radius = 20.0

[node name="Root" type="Node2D"]

[node name="InactiveArea" type="Area2D" parent="."]
monitoring = false
monitorable = false

[node name="Shape" type="CollisionShape2D" parent="InactiveArea"]
shape = SubResource("CircleShape2D_1")
