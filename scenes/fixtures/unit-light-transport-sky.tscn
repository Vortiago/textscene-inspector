; Sky ambient (image-based light), isolated: the third way energy reaches a surface, after
; `unit-light-transport-direct` and `-ambient`. All four ProceduralSkyMaterial colours are white,
; so irradiance is 1.0 on any normal and a Lambertian surface renders its own albedo. The
; unshaded square is that albedo, invisible when the IBL scale is right.
[gd_scene load_steps=8 format=3]

[sub_resource type="ProceduralSkyMaterial" id="SkyMat_1"]
sky_top_color = Color(1, 1, 1, 1)
sky_horizon_color = Color(1, 1, 1, 1)
ground_bottom_color = Color(1, 1, 1, 1)
ground_horizon_color = Color(1, 1, 1, 1)

[sub_resource type="Sky" id="Sky_1"]
sky_material = SubResource("SkyMat_1")

[sub_resource type="Environment" id="Env_1"]
background_mode = 2
sky = SubResource("Sky_1")
ambient_light_source = 3
ambient_light_sky_contribution = 1.0
tonemap_mode = 0

[sub_resource type="PlaneMesh" id="Ground_1"]
size = Vector2(8, 8)

[sub_resource type="PlaneMesh" id="Patch_1"]
size = Vector2(2.5, 2.5)

[sub_resource type="StandardMaterial3D" id="Lit_1"]
albedo_color = Color(0.5, 0.5, 0.5, 1)
metallic = 0.0
roughness = 1.0
diffuse_mode = 1

[sub_resource type="StandardMaterial3D" id="Unshaded_1"]
albedo_color = Color(0.5, 0.5, 0.5, 1)
shading_mode = 0

[node name="LightTransportSky" type="Node3D"]

; Uniformity removes the filter shape, so this measures only the IBL scale. three's PMREM
; roughness-1 mip and Godot's roughest radiance mip are different convolutions that disagree by
; a few /255 on a graded sky, which would look like a scale error.
[node name="WorldEnvironment" type="WorldEnvironment" parent="."]
environment = SubResource("Env_1")

; Energy 0 suppresses Godot's editor preview sun (ADR-0025 counts node types, not energy) and
; leaves the sky shader's sun disc unlit, so the sky stays uniform.
[node name="DarkSun" type="DirectionalLight3D" parent="."]
light_energy = 0.0

[node name="LitGround" type="MeshInstance3D" parent="."]
mesh = SubResource("Ground_1")
surface_material_override/0 = SubResource("Lit_1")

[node name="UnshadedReference" type="MeshInstance3D" parent="."]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.01, 0)
mesh = SubResource("Patch_1")
surface_material_override/0 = SubResource("Unshaded_1")
