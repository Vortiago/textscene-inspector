[gd_scene format=3]

; CCDIK3D under the Skeleton3D it modifies, the only placement where it does anything. It binds no
; property of its own, so every key is an inherited one at a non-default value. angular_delta_limit
; is stored in radians against a hint in degrees: 0.0872665 is 5 degrees.

[node name="Root" type="Node3D"]

[node name="Skeleton3D" type="Skeleton3D" parent="."]

[node name="MyCCDIK3D" type="CCDIK3D" parent="Skeleton3D"]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 1, 0)
influence = 0.75
mutable_bone_axes = false
max_iterations = 20
min_distance = 0.005
angular_delta_limit = 0.0872665
deterministic = true
