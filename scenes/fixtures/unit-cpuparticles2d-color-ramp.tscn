[gd_scene load_steps=4 format=3]

; Both Gradient slots. `color_ramp` is sampled by age, so one particle changes colour as it travels.
; `color_initial_ramp` is sampled once at birth, so it varies between particles. The two emitters
; differ only in the slot.

[ext_resource type="Texture2D" path="res://textures/pong-ball.png" id="1_ball"]

[sub_resource type="Gradient" id="Gradient_age"]
offsets = PackedFloat32Array(0, 0.5, 1)
colors = PackedColorArray(1, 1, 1, 1, 0.1, 0.6, 1, 1, 0.4, 0, 0.6, 0)

[sub_resource type="Gradient" id="Gradient_birth"]
offsets = PackedFloat32Array(0, 1)
colors = PackedColorArray(1, 0.2, 0.1, 1, 0.2, 1, 0.4, 1)

[node name="Root" type="Node2D"]

[node name="Backdrop" type="Polygon2D" parent="."]
color = Color(0.08, 0.08, 0.1, 1)
polygon = PackedVector2Array(320, 68, 832, 68, 832, 580, 320, 580)

[node name="ByAge" type="CPUParticles2D" parent="."]
position = Vector2(440, 200)
amount = 32
texture = ExtResource("1_ball")
lifetime = 2.0
preprocess = 2.0
use_fixed_seed = true
seed = 1234
fixed_fps = 30
local_coords = true
direction = Vector2(1, 0)
spread = 0.0
gravity = Vector2(0, 0)
initial_velocity_min = 140.0
initial_velocity_max = 140.0
scale_amount_min = 4.0
scale_amount_max = 4.0
color_ramp = SubResource("Gradient_age")

[node name="ByBirth" type="CPUParticles2D" parent="."]
position = Vector2(440, 450)
amount = 32
texture = ExtResource("1_ball")
lifetime = 2.0
preprocess = 2.0
use_fixed_seed = true
seed = 1234
fixed_fps = 30
local_coords = true
direction = Vector2(1, 0)
spread = 0.0
gravity = Vector2(0, 0)
initial_velocity_min = 140.0
initial_velocity_max = 140.0
scale_amount_min = 4.0
scale_amount_max = 4.0
color_initial_ramp = SubResource("Gradient_birth")
