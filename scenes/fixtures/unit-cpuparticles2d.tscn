[gd_scene load_steps=3 format=3]

; A steady CPUParticles2D plume. Everything that would otherwise vary between
; runs is pinned: use_fixed_seed + seed, an explicit preprocess, and fixed_fps.

[ext_resource type="Texture2D" path="res://textures/pong-ball.png" id="1_ball"]

[sub_resource type="Gradient" id="Gradient_flame"]
offsets = PackedFloat32Array(0, 0.5, 1)
colors = PackedColorArray(1, 0.9, 0.4, 1, 1, 0.45, 0.05, 1, 0.6, 0.1, 0, 0)

[node name="Root" type="Node2D"]

[node name="Backdrop" type="Polygon2D" parent="."]
color = Color(0.08, 0.08, 0.1, 1)
polygon = PackedVector2Array(320, 68, 832, 68, 832, 580, 320, 580)

[node name="Plume" type="CPUParticles2D" parent="."]
position = Vector2(576, 480)
amount = 48
texture = ExtResource("1_ball")
lifetime = 1.5
preprocess = 1.5
use_fixed_seed = true
seed = 4242
fixed_fps = 30
local_coords = true
emission_shape = 1
emission_sphere_radius = 16.0
direction = Vector2(0, -1)
spread = 20.0
gravity = Vector2(0, -60)
initial_velocity_min = 90.0
initial_velocity_max = 150.0
scale_amount_min = 1.5
scale_amount_max = 4.0
color_ramp = SubResource("Gradient_flame")
