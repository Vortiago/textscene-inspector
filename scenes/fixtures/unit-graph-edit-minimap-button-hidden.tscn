; GraphEdit with only the minimap toggle button hidden. `show_minimap_button` hides the toolbar
; button alone (graph_edit.cpp:2854-2857), while `minimap_enabled` decides the panel. A renderer
; that conflates the two draws the right picture wherever neither moves, and the wrong one here.

; Every fixture in this family shares one GraphEdit rect and one GraphNode, and only
; unit-graph-edit-scroll-offset-clamped.tscn authors `scroll_offset`: `set_scroll_offset` clamps
; against bounds no child has widened while properties apply (graph_edit.cpp:407), so an authored
; offset moves the grid, the nodes and the minimap camera together and masks the one variable.

[gd_scene load_steps=1 format=3 uid="uid://unitgraphedminbtn"]

[node name="Root" type="Control"]
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0

[node name="Graph" type="GraphEdit" parent="."]
layout_mode = 1
offset_left = 8.0
offset_top = 8.0
offset_right = 408.0
offset_bottom = 328.0
show_minimap_button = false

[node name="Source" type="GraphNode" parent="Graph"]
offset_right = 120.0
offset_bottom = 64.0
position_offset = Vector2(40, 56)
title = "Add"

[node name="Value" type="Label" parent="Graph/Source"]
layout_mode = 2
text = "Value"
