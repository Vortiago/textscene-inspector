[gd_scene format=3 uid="uid://edge_light_invalid_color"]

; `light_color` with components below 0 and above 1. v.color checks only the Color(r,g,b,a)
; shape, so this lints clean. THREE.Color.setRGB takes any float and the framebuffer clamps,
; as Godot's HDR light colour does.

[node name="Scene" type="Node3D"]

[node name="OddColorLight" type="OmniLight3D" parent="."]
light_color = Color(-1, 2, 5, 1)
omni_range = 10.0
