[gd_scene load_steps=2 format=3]

; The two settings the frozen pose cannot follow, each raising an advisory
; warning rather than an error — both are perfectly legal Godot. RING samples
; the engine's process-wide RNG, which no scene file carries, so the previewer
; emits from the node origin instead; `fract_delta` sub-steps a restarting
; particle, which a fixed-rate evaluation skips.

[ext_resource type="Texture2D" path="res://textures/pong-ball.png" id="1_ball"]

[node name="Root" type="Node2D"]

[node name="Backdrop" type="Polygon2D" parent="."]
color = Color(0.08, 0.08, 0.1, 1)
polygon = PackedVector2Array(320, 68, 832, 68, 832, 580, 320, 580)

[node name="RingEmitter" type="CPUParticles2D" parent="."]
position = Vector2(576, 324)
amount = 48
texture = ExtResource("1_ball")
lifetime = 1.5
preprocess = 1.5
use_fixed_seed = true
seed = 5
fixed_fps = 30
fract_delta = true
local_coords = true
emission_shape = 6
emission_ring_radius = 120.0
emission_ring_inner_radius = 90.0
gravity = Vector2(0, 0)
scale_amount_min = 3.0
scale_amount_max = 3.0
color = Color(1, 0.85, 0.3, 1)
