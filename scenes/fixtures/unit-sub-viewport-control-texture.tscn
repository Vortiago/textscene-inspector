; One variable: where a sub-viewport's Control pixels come from and which colour pipeline they
; cross. Controls only, so the Control rect solve paints the offscreen target, sampled back as a
; ViewportTexture. No other scene puts Controls behind a ViewportTexture.

[gd_scene format=3]

[sub_resource type="QuadMesh" id="QuadMesh_1"]
size = Vector2(2, 2)

[sub_resource type="ViewportTexture" id="ViewportTexture_1"]
viewport_path = NodePath("SubViewport")

[sub_resource type="StandardMaterial3D" id="StandardMaterial3D_1"]
resource_local_to_scene = true
shading_mode = 0
albedo_texture = SubResource("ViewportTexture_1")

; Not gated, because of a live divergence: the backdrop measures rgb(162) against Godot 4.6.3,
; but the Panel's translucent fill reads 110 where Godot reads 84. That fill is
; `style_normal_color`, Color(0.1, 0.1, 0.1, 0.6) (`default_theme.cpp:112,134`), and the same
; colour on the main canvas (a Button's `normal` box, `:138`) measures 45 against Godot's 46.
[node name="Root" type="Node3D"]

; So the alpha blend is right and the offscreen target diverges: an sRGB-tagged attachment is
; linearised before blending, while Godot's target holds sRGB canvas values and blends in them.
; A baseline taken now would pin that gap instead of gating against it.
[node name="SubViewport" type="SubViewport" parent="."]
size = Vector2i(256, 256)
render_target_update_mode = 4

; Measured through Godot 4.6.3 under the editor preview environment (FILMIC): the backdrop's
; Color(0.5, 0.5, 0.5) reaches the quad as rgb(162). Tonemapping the raster too would land it
; near rgb(196).
[node name="GUI" type="Control" parent="SubViewport"]
anchors_preset = 0
offset_right = 256.0
offset_bottom = 256.0

; Flat, opaque and mid-grey: the probe that separates one tonemap from two. Godot draws a
; viewport's canvas after its tonemap (`RendererViewport::_draw_viewport` runs `_draw_3d`, which
; ends in `_render_buffers_post_process_and_tonemap`, before the `render_canvas` loop), so the
; target holds untonemapped sRGB and the curve applies once, on the quad in the main viewport.
[node name="Backdrop" type="ColorRect" parent="SubViewport/GUI"]
anchors_preset = 0
offset_right = 256.0
offset_bottom = 256.0
color = Color(0.5, 0.5, 0.5, 1)

; Panel and Label carry default-theme chrome and a glyph run, so the target also meets a
; translucent StyleBox fill and shaped text, not one flat fill.
[node name="Panel" type="Panel" parent="SubViewport/GUI"]
anchors_preset = 0
offset_left = 32.0
offset_top = 32.0
offset_right = 224.0
offset_bottom = 112.0

[node name="Label" type="Label" parent="SubViewport/GUI"]
anchors_preset = 0
offset_left = 32.0
offset_top = 160.0
offset_right = 224.0
offset_bottom = 192.0
text = "VIEWPORT"
horizontal_alignment = 1

[node name="Screen" type="MeshInstance3D" parent="."]
mesh = SubResource("QuadMesh_1")
surface_material_override/0 = SubResource("StandardMaterial3D_1")
