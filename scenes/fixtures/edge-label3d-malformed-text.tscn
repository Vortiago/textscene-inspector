[gd_scene format=3 uid="uid://edge_label3d_malformed_text"]

; Lint-safe Label3D edge cases: `text = ""` (empty is the serialised default
; and renders as a blank label) and `pixel_size` at the bottom of its hint
; (0.0001 - vanishingly small, effectively invisible at normal camera distance,
; without being <= 0). The scene lints clean.

[node name="Scene" type="Node3D"]

[node name="EmptyText" type="Label3D" parent="."]
text = ""
billboard = 1
pixel_size = 0.0001
