{
  "name": "@textscene/web-previewer",
  "version": "0.0.1",
  "description": "Web-based text scene file inspector for development and debugging",
  "type": "module",
  "scripts": {
    "copy-fixtures": "node scripts/copy-fixtures.js",
    "predev": "pnpm copy-fixtures && pnpm --filter @textscene/renderer build",
    "dev": "vite",
    "prebuild": "pnpm copy-fixtures && pnpm --filter @textscene/renderer build",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "pretype-check": "pnpm --filter @textscene/renderer build",
    "type-check": "tsc --noEmit",
    "clean": "rimraf dist node_modules/.vite public/fixtures"
  },
  "keywords": [
    "tscn",
    "textscene",
    "text-scene",
    "inspector",
    "web",
    "3d"
  ],
  "author": "Vortiago",
  "license": "MIT",
  "dependencies": {
    "@textscene/core": "workspace:*"
  },
  "devDependencies": {
    "rimraf": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
