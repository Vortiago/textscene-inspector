name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

# A new push to a pull request cancels its older run. Each push to main runs to the end.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  static:
    name: Lint, type check and generated files
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup

      - run: pnpm lint
      - run: pnpm type-check

      # The test projects resolve @textscene/core through its emitted declarations, and the
      # docs checks load its dist.
      - run: pnpm --filter @textscene/core build
      - run: pnpm type-check:tests

      - run: pnpm docs:lint-sections --check
      - run: pnpm docs:index --check
      # After the sheets, which the gallery is assembled from.
      - run: pnpm docs:gallery --check
      - run: pnpm fonts:bake --check

  unit:
    name: Unit tests (${{ matrix.shard }}/4)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4]
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup

      # Before the tests: loadCoreLinter.test.mjs skips itself when core's dist is absent.
      - run: pnpm --filter @textscene/core build
      - run: pnpm test:unit --shard=${{ matrix.shard }}/4

  build:
    name: Build and package
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup

      # The prebuild hook builds @textscene/core.
      - run: pnpm --filter @textscene/web-previewer build
      - run: pnpm build:linter

      # Lints with the binary this run built. The vendored demos are scenes Godot itself
      # opens, so an error here is a false positive in a rule.
      - run: pnpm lint:scenes:only

      - run: pnpm vsc:package

      # Reads the webview and extension host bundles from the packaging step.
      - run: pnpm check:bundle-size
      - run: pnpm check:package

      - uses: actions/upload-artifact@v7
        with:
          name: web-previewer
          path: apps/textscene-web/dist/

      - uses: actions/upload-artifact@v7
        with:
          name: vscode-extension
          path: apps/textscene-vscode/*.vsix

      - uses: actions/upload-artifact@v7
        with:
          name: tscn-linter-cli
          path: apps/textscene-linter/dist/cli.js

  visual-regression:
    name: Visual regression (${{ matrix.shard }}/6)
    runs-on: ubuntu-latest
    needs: build
    timeout-minutes: 20
    # The harness captures one scene at a time with software GL, and the whole set takes more
    # than 30 minutes on one runner.
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4, 5, 6]
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup

      # assertWebBuildFresh compares core's src with core's own dist before it captures from
      # a downloaded bundle, so the dist must exist here.
      - run: pnpm --filter @textscene/core build

      # The artifact is this commit's build. VISUAL_SKIP_BUILD=1 keeps it, and a rebuild
      # would empty the downloaded dist.
      - uses: actions/download-artifact@v8
        with:
          name: web-previewer
          path: apps/textscene-web/dist/

      - uses: actions/cache@v6
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}
      - run: npx playwright install chromium --with-deps

      - run: pnpm test:visual --shard ${{ matrix.shard }}/6
        env:
          VISUAL_SKIP_BUILD: '1'

      - if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: visual-diffs-${{ matrix.shard }}
          path: scripts/visual/output/

  e2e-web:
    name: E2E (web previewer)
    runs-on: ubuntu-latest
    needs: build
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup

      # The same build reuse as in visual-regression.
      - run: pnpm --filter @textscene/core build
      - uses: actions/download-artifact@v8
        with:
          name: web-previewer
          path: apps/textscene-web/dist/

      - uses: actions/cache@v6
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}
      - run: npx playwright install chromium --with-deps

      - run: pnpm test:e2e:web
        env:
          VISUAL_SKIP_BUILD: '1'
          # The runner has no system Chrome, so the gate uses Playwright's bundled Chromium.
          SHOWCASE_CHANNEL: bundled

  integration:
    name: Integration tests (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup

      # textscene-inspector is the VS Code extension package (apps/textscene-vscode).
      - if: runner.os == 'Linux'
        run: xvfb-run -a pnpm --filter textscene-inspector test:integration
      - if: runner.os != 'Linux'
        run: pnpm --filter textscene-inspector test:integration

      # Proves that text paints inside the webview, which the extension host suite cannot
      # see. The driver starts its own xvfb-run and uses VS Code's Chromium. It runs on
      # Linux only, because nobody has verified its window and binary layout elsewhere.
      - if: runner.os == 'Linux'
        run: pnpm test:vscode:csp

      - if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: integration-test-results-${{ matrix.os }}
          path: |
            apps/textscene-vscode/.test-workspace/
            apps/textscene-vscode/out/
            scripts/vscode/output/csp-gate/

  vsix:
    name: VSIX contents
    runs-on: ubuntu-latest
    needs: build
    timeout-minutes: 5
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: vscode-extension
          path: .

      - name: Check the packaged files
        run: |
          vsix=$(ls *.vsix)
          for entry in extension/dist/extension.js extension/dist/extension.web.js \
            extension/package.json extension.vsixmanifest; do
            unzip -l "$vsix" | grep -q "$entry" || { echo "::error::$entry is missing from $vsix"; exit 1; }
          done
          # The browser entry point runs the extension on vscode.dev.
          unzip -p "$vsix" extension/package.json | grep -q '"browser"' \
            || { echo "::error::the packaged package.json has no browser entry"; exit 1; }

  # The one check that branch protection requires, so a new or renamed job needs no settings change.
  ci-ok:
    name: CI passed
    if: always()
    needs: [static, unit, build, visual-regression, e2e-web, integration, vsix]
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - if: contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') || contains(needs.*.result, 'skipped')
        run: exit 1
