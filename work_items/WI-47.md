# WI-47: Extract Light Base Property Parsing (Optional)

**Phase**: 12 - Code Quality & Tooling Improvements
**Priority**: Low
**Status**: Complete
**Type**: Refactoring

## Summary

Extract shared light property parsing logic to reduce duplication across light types.

## Implementation Details

- Create `packages/textscene-renderer/src/utils/lightParser.ts`
- Implement `parseLightBaseProperties(properties)` function
  - Parse light_color, light_energy, shadow_enabled, shadow_bias, shadow_filter
  - Return typed object with base light properties
- Refactor directionallight3d/omnilight3d/spotlight3d parsers to use shared function
- Each light parser calls base function then adds light-specific properties
- Add tests for shared parsing logic

## Value Proposition

Reduces ~45 lines of duplication across 3 light types.

## Notes

Code quality improvement, not critical functionality. Apply Rule of Three - only extract if duplication is clear.

## Testing Notes

- Test all three light types still parse correctly
- Verify no regression in light rendering
- Test edge cases (missing properties)
