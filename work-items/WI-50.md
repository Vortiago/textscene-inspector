# WI-50: VSCode Outline Provider Integration

**Phase**: 6 - VS Code Extension UX Improvements
**Priority**: Medium
**Status**: Not Started
**Type**: Feature

## Summary

Implement DocumentSymbolProvider for native VS Code outline/breadcrumbs integration without duplicating webview tree.

## Implementation Details

- Implement `DocumentSymbolProvider` for .tscn files
- Register provider with VS Code language API
- Parse TSCN files using existing `parseTscn()` from `@textscene/renderer`
- Map TSCN node hierarchy to VS Code `DocumentSymbol` objects
- Assign appropriate `SymbolKind` for different node types (MeshInstance3D → Class, properties → Property, etc.)
- Support breadcrumbs navigation in editor
- Enable "Go to Symbol" (Ctrl+Shift+O) quick navigation
- Enable Outline view in sidebar showing scene hierarchy

## Value Proposition

Native VS Code integration without duplicating webview tree, lightweight (~100 lines).

## Benefits

Complements webview tree with native navigation features (breadcrumbs, outline panel, symbol search).

## Testing Notes

- Test with simple scenes (single node)
- Test with complex hierarchies (nested nodes)
- Verify breadcrumbs update correctly
- Verify outline view updates on file changes
- Test "Go to Symbol" command

## References

- VS Code DocumentSymbolProvider API
- VS Code SymbolKind enum
